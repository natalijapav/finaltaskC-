@model IEnumerable<APISystem.Entity.Predmet>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Naziv)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Profesor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Departmant)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Naziv)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Profesor.OsobaId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Departmant.DepartmantId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PredmetId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PredmetId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PredmetId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
